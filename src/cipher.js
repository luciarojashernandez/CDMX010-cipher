/*const cipher = {
  encode: function() {
    let text = 'adriana';
    for(let i = 0; i < text.length; i++){
      console.log(text[i]);
    }
  },
  decode
};

export default cipher;*/

/*const cipher = {
  function encode13(texto) {
    for (let i = 0; i < textp.length; i ++)
    console.log (texto[i]);
    
  }
}

encode13 ("A")*/
