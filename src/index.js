/*import cipher from './cipher.js';

console.log(cipher);*/

const cipher = {
    function encode13(texto) {
      for (let i = 0; i < textp.length; i ++)
      console.log (texto[i]);
      
    }
  }
  
  encode13 ("A")
